node_modules/
dist/
export/
.vercel/
.env
.env*.local
# catch stray copies too (.env.production.local.save etc.) — public repo, no leaks
.env*
*.log
.DS_Store
.vercel

# Personal / job-application artifacts — never part of the public showcase
*.docx
job-applications/
CASE_LinkedIn_Saver.md
build_case_docx.cjs

# AI-tooling scaffolding — kept local, not product code
.qwen/
.superpowers/

# Claude Code personal config — shared agents in .claude/agents/ stay tracked
.claude/settings.local.json
.claude/launch.json

# Chrome Web Store package (generated by npm run extension:package)
linkedin-saver-extension.zip
